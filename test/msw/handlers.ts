import type { RequestHandler } from 'msw';

// Empty in the initial scaffold. The first API-touching feature spec will
// add resource-specific handlers here.
export const handlers: RequestHandler[] = [];
