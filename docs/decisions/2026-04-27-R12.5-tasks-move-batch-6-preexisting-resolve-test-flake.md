# Decision 6 — Pre-existing flake in `test/config/resolve.test.ts` (re-confirmed)

**Run:** 2026-04-27-R12.5-tasks-move-batch
**Phase:** 4 (test) — orchestrator
**Agent:** orchestrator

**Question:** Full test suite has one failing test
(`test/config/resolve.test.ts > buildSourceMap — source attribution > all
sources are default when nothing is set`). Is this introduced by R12.5?

**Decision:** **Pre-existing**, machine-state-dependent flake. Documented in
`docs/runs/2026-04-27-1732-tasks-move/decisions/01-preexisting-resolve-test-flake.md`
during the R12 run. Same failure, same machine, same root cause.

**Alternatives considered:**

- Fix the flake as part of this run — rejected: out of scope. R12.5 has nothing
  to do with `src/config/resolve.ts`.
- Pause the run — rejected: the failure is environmental (CI runners pass
  cleanly because they don't have the dev machine's `%APPDATA%/freelo-cli-nodejs/Config/`
  store). The R12 run merged successfully through this same flake; R12.5 is in
  the same boat.

**Rationale:**

1. R12.5 only touched `src/api/schemas/task.ts`, `src/commands/tasks/move.ts`,
   `test/commands/tasks/move.test.ts`, and added two fixtures + two docs files
   + a changeset. None of these intersect with `src/config/resolve.ts` or its
   test.
2. The R12 run shipped through the same flake; PR #50's CI was 7-of-7 green.
3. Following Calibration §3, the gates that matter for the merge decision are
   `pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm check:readme`
   on the clean **committed** tree as run by CI — where this test is known to
   pass. Local dev-machine state is informational, not blocking.

Follow-up tracked in run summary: refactor the test to mock `conf` (like
`tasks/move.test.ts` does) so it stops reading real `%APPDATA%`.
