# Phase 7 — Document

`.claude/docs/sdlc.md` Phase 6 — appended autogen-block rule:

> The published `README.md` carries an autogen Commands block between
> `<!-- BEGIN AUTOGEN COMMANDS -->` / `<!-- END AUTOGEN COMMANDS -->` markers...
> doc-writer must run `pnpm fix:readme` whenever a spec adds, removes, renames,
> or re-describes a public command.

`docs/getting-started.md` — added a one-line cross-link from "Next steps" to the README's auto-checked Commands section.

No new per-command pages — this run adds tooling, not commands.
