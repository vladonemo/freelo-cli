# Phase 2 — Spec

Spec written: `docs/specs/0008-help-in-introspect-data.md` (≤80 lines net,
ignoring code blocks).

Note on numbering: three concurrent runs collided on `0007`. This run renumbers
to `0008`. The transient `docs/specs/0007-help-in-introspect-data.md` stub is
left as a redirect note (the orchestrator cannot delete files via Bash).
