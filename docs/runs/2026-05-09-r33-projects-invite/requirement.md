# R33 — `freelo projects invite` (Wave 5, fifth slice)

Original input: see `/auto` invocation. Roadmap entry `docs/roadmap.md` §R33:

- Endpoint: `POST /users/manage-workers`
- CLI surface: `freelo projects invite --project <id>... (--email <e> | --user <id>)...`
- Depends on: R32 (`bc90b43`)
- Tier expectation: Yellow

Run parameters: allowNetwork=false, autoShip=false, default budget.

Calibration §3 amendment binding: build → fix:readme → check:readme with no source edits in between.
