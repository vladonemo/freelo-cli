# Spec 0007 — superseded (renumbered to 0008)

This file was a transient artifact of a numbering collision (three concurrent
runs all picked 0007). The intended content lives at
`docs/specs/0008-help-in-introspect-data.md`.

This stub is left in place because the orchestrator cannot delete files; the
`docs/specs/0008-...` file is the canonical spec.
